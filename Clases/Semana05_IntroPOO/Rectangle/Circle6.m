% Definiendo un propiedad constante

classdef Circle6
    properties(Constant)
        pi = 3.14
    end
end

%%
% Circle.pi

% Una clase en MATLAB puede tener una propiedad constante (o más de una), 
% es decir, una propiedad cuyo valor nunca se modifica después de la primera asignación:
